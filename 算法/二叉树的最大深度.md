![avatar](/image/depth.png)
![avatar](/image/depth1.png)
```javascript
var maxDepth = function(root) {
    if(!root) {
        return 0;
    } else {
        const left = maxDepth(root.left);
        const right = maxDepth(root.right);
        return Math.max(left, right) + 1;
    }
};

```
